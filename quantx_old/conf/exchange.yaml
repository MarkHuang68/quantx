# 檔案: conf/exchange.yaml
# 版本: v2 (Taker/Maker 費用分離)

exchanges:
  bybit:
    # 🟢 將單一的 fee_bps 拆分為 Taker 和 Maker
    taker_fee_bps: 5.5       # Taker (吃單) 手續費 (單位: BPS, 1 BPS = 0.01%)
    maker_fee_bps: 2.0       # Maker (掛單) 手續費
    slip_bps: 1.0            # 模擬滑點 (BPS)
    min_qty: 0.001           # 最小下單數量
    min_notional: 5          # 最小下單金額
  binance:
    taker_fee_bps: 4.0
    maker_fee_bps: 2.0
    slip_bps: 1.0
    min_qty: 0.0001
    min_notional: 10
  coinbase:
    taker_fee_bps: 15.0
    maker_fee_bps: 10.0
    slip_bps: 5.0
    min_qty: 0.001
    min_notional: 1